#!/bin/bash

MNT=/media/br0ns/backup
TARGET=/home/br0ns/

BUP_DIR="$MNT/bup/"
HOST="$(hostname)"

if ! mount | grep -q "$MNT" ; then
    echo "$MNT is not mounted"
    exit 1
fi

BUP_DIR=$BUP_DIR sudo -E bup index -ux "$TARGET"
BUP_DIR=$BUP_DIR sudo -E bup save -n "$HOST" "$TARGET"
